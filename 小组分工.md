产品
负责系统业务核心层与三算法页公共 UI 的统一实现，完成边界为 src/core/（StepRecorder、PlaybackEngine）、src/composables/useAlgorithmRunner.js 及 src/components/common/ 下 AlgorithmPage、ControlPanel、StepInfoPanel、PlaybackController、TestCaseList 五个组件；需制定并文档化 generateSteps(input) 统一步骤接口规范，供成员 3～5 的算法模块遵循。具体工作包括：实现步骤记录与单步/自动播放/暂停/重置/回退（扩展）播放逻辑，封装算法运行与会话状态管理，统一三算法页左中右布局与按钮交互，汇总维护 src/data/testCases.json 共 6 条用例，编写 README.md 运行说明及系统测试文档。不负责各算法具体逻辑与专属可视化组件，不负责工程脚手架与路由初始化。验收标准：三算法页可共用公共组件运行，播放控制可用，6 条用例可加载，README 可复现 npm run dev / npm run build。

---

前端

· 负责工程脚手架搭建、全站路由导航与最终联调集成，完成边界为项目根目录配置及 src/router/、src/views/HomeView.vue、src/components/common/AppHeader.vue、src/styles/、src/data/algorithms.json、src/App.vue、src/main.js。具体工作包括：初始化 Vue 3 + Vite 项目，配置 Hash 路由（首页 + 三条算法路由），实现统一入口首页算法卡片与顶栏导航（含「首页」链接），编写三条算法元信息（名称、类型、难度、复杂度、简介），制定全局样式规范保证三页视觉一致，合并各成员分支代码并完成联调，确保 npm run build 产出可静态部署。不负责算法步骤生成、可视化绘制及公共播放组件内部逻辑。验收标准：工程可正常启动与构建，4 个页面均可访问，首页与顶栏导航可用，全站风格统一无布局错乱。
· 负责排序类可视化组件：src/components/visualizers/ArrayVisualizer.vue、src/views/QuickSortView.vue；用 Canvas 柱状图展示数组状态及 pivot/比较高亮；接入 AlgorithmPage 公共布局与播放控制；支持逗号或空格分隔整数的手动输入（长度 2～20）与一键随机生成。
· 负责图算法类可视化组件：src/components/visualizers/GraphVisualizer.vue、src/views/DijkstraView.vue；用 SVG 绘制节点-边图，高亮当前扩展节点与松弛边；支持有向/无向带权图输入（节点数、边列表、源点、目标点）及预设用例加载。
· 负责递归类可视化组件：src/components/visualizers/HanoiVisualizer.vue、src/views/HanoiView.vue；用 Canvas 绘制三根柱及圆盘堆叠，展示移动前后状态；支持盘子数 n 手动输入（范围 3～8）与预设用例；终态展示总步数 2ⁿ−1 及完整移动序列。

---

后端

· 负责排序类算法逻辑：src/algorithms/quickSort.js，实现快速排序 generateSteps，对选 pivot、分区比较、元素交换、递归子区间等关键步骤逐步 record 并附中文说明；src/utils/validators.js 中 validateQuickSortInput、src/utils/generators.js 中快排随机数组生成函数。不负责 Dijkstra、汉诺塔模块及公共组件、PlaybackEngine 核心代码。
· 负责图算法类逻辑：src/algorithms/dijkstra.js，实现 Dijkstra 单源最短路径 generateSteps，对初始化距离表、选取当前节点、边松弛、前驱更新等步骤逐步 record；src/utils/validators.js 中 validateGraphInput、src/utils/generators.js 中随机图生成函数。不负责 快排、汉诺塔模块及公共框架代码。
· 负责递归类算法逻辑：src/algorithms/hanoi.js，实现汉诺塔递归 generateSteps，每次合法盘子移动记录一步并附递归分解说明；src/utils/validators.js 中 validateHanoiInput、src/utils/generators.js 中相关辅助函数。不负责 快排、Dijkstra 模块及公共组件、项目集成联调。

---

测试

· 在 testCases.json 中提交排序类用例 TC-S-01、TC-S-02 两条。
· 在图算法类中提交 TC-G-01、TC-G-02 两条用例，TC-G-02 须覆盖目标不可达场景并正确显示 ∞ 或不可达提示。
· 提交汉诺塔用例 TC-H-01（n=3，7 步）、TC-H-02（n=4，15 步）两条。
· 验收标准：
  · 三算法页可共用公共组件运行，播放控制可用，6 条用例可加载，README 可复现 npm run dev / npm run build。
  · 工程可正常启动与构建，4 个页面均可访问，首页与顶栏导航可用，全站风格统一无布局错乱。
  · 排序结果正确，过程分步可视化完整，2 条测试用例通过，非法输入有友好提示。
  · 最短距离与路径正确，图可视化随步骤更新，2 条测试用例通过，含不可达节点场景。
  · 移动序列符合汉诺塔规则，逐步展示完整，n=3 为 7 步、n=4 为 15 步，2 条测试用例通过。