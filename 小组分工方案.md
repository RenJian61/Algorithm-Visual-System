# 算法过程可视化系统 — 5 人小组分工方案

| 项目 | 内容 |
|------|------|
| 项目名称 | 算法过程可视化系统（AVS） |
| 小组编号 | ____________ |
| 编写日期 | 2026-06-19 |
| 技术路线 | Vue 3 + Vite + JavaScript，Canvas / SVG，Hash 路由 |
| 算法选型 | 快速排序 · Dijkstra 最短路径 · 汉诺塔 |

---

## 注意事项

1. **小组分工方案一经确认提交，将不可修改**，系统留存分工记录作为课程评分重要依据。
2. **分工内容直接关联学生个人最终成绩**，指导教师将根据分工核查对应成员的完成主体与质量匹配度。
3. 分工内容请务必**详实具体**，需明确标注成员负责的**模块、工作内容与完成边界**，禁止模糊表述。
4. 请团队成员**充分沟通**，确认分工细节后，再完成保存与确认操作。

---

## 小组分工总表

| 序号 | 姓名 | 学号 | 角色 | 分工说明 |
|:----:|------|------|------|----------|
| **1** | | | **核心引擎与公共组件负责人** | 负责系统业务核心层与三算法页公共 UI 的统一实现，完成边界为 `src/core/`（StepRecorder、PlaybackEngine）、`src/composables/useAlgorithmRunner.js` 及 `src/components/common/` 下 AlgorithmPage、ControlPanel、StepInfoPanel、PlaybackController、TestCaseList 五个组件；需制定并文档化 `generateSteps(input)` 统一步骤接口规范，供成员 3～5 的算法模块遵循。具体工作包括：实现步骤记录与单步/自动播放/暂停/重置/回退（扩展）播放逻辑，封装算法运行与会话状态管理，统一三算法页左中右布局与按钮交互，汇总维护 `src/data/testCases.json` 共 6 条用例，编写 `README.md` 运行说明及系统测试文档。**不负责**各算法具体逻辑与专属可视化组件，**不负责**工程脚手架与路由初始化。验收标准：三算法页可共用公共组件运行，播放控制可用，6 条用例可加载，README 可复现 `npm run dev` / `npm run build`。 |
| **2** | | | **项目框架与集成负责人** | 负责工程脚手架搭建、全站路由导航与最终联调集成，完成边界为项目根目录配置及 `src/router/`、`src/views/HomeView.vue`、`src/components/common/AppHeader.vue`、`src/styles/`、`src/data/algorithms.json`、`src/App.vue`、`src/main.js`。具体工作包括：初始化 Vue 3 + Vite 项目，配置 Hash 路由（首页 + 三条算法路由），实现统一入口首页算法卡片与顶栏导航（含「首页」链接），编写三条算法元信息（名称、类型、难度、复杂度、简介），制定全局样式规范保证三页视觉一致，合并各成员分支代码并完成联调，确保 `npm run build` 产出可静态部署。**不负责**算法步骤生成、可视化绘制及公共播放组件内部逻辑。验收标准：工程可正常启动与构建，4 个页面均可访问，首页与顶栏导航可用，全站风格统一无布局错乱。 |
| **3** | | | **快速排序算法负责人** | 负责排序类算法从输入到可视化的完整链路，完成边界为 `src/algorithms/quickSort.js`、`src/components/visualizers/ArrayVisualizer.vue`、`src/views/QuickSortView.vue`，以及 `src/utils/validators.js` 中 `validateQuickSortInput`、`src/utils/generators.js` 中快排随机数组生成函数，并在 `testCases.json` 中提交 TC-S-01、TC-S-02 两条用例。具体工作包括：实现快速排序 `generateSteps`，对选 pivot、分区比较、元素交换、递归子区间等关键步骤逐步 `record` 并附中文说明；用 Canvas 柱状图展示数组状态及 pivot/比较高亮；接入 AlgorithmPage 公共布局与播放控制；支持逗号或空格分隔整数的手动输入（长度 2～20）与一键随机生成。**不负责** Dijkstra、汉诺塔模块及公共组件、PlaybackEngine 核心代码。验收标准：排序结果正确，过程分步可视化完整，2 条测试用例通过，非法输入有友好提示。 |
| **4** | | | **Dijkstra 算法负责人** | 负责图算法类从输入到可视化的完整链路，完成边界为 `src/algorithms/dijkstra.js`、`src/components/visualizers/GraphVisualizer.vue`、`src/views/DijkstraView.vue`，以及 `src/utils/validators.js` 中 `validateGraphInput`、`src/utils/generators.js` 中随机图生成函数，并在 `testCases.json` 中提交 TC-G-01、TC-G-02 两条用例。具体工作包括：实现 Dijkstra 单源最短路径 `generateSteps`，对初始化距离表、选取当前节点、边松弛、前驱更新等步骤逐步 `record`；用 SVG 绘制节点-边图，高亮当前扩展节点与松弛边；支持有向/无向带权图输入（节点数、边列表、源点、目标点）及预设用例加载；TC-G-02 须覆盖目标不可达场景并正确显示 ∞ 或不可达提示。**不负责** 快排、汉诺塔模块及公共框架代码。验收标准：最短距离与路径正确，图可视化随步骤更新，2 条测试用例通过，含不可达节点场景。 |
| **5** | | | **汉诺塔算法负责人** | 负责递归类算法从输入到可视化的完整链路，完成边界为 `src/algorithms/hanoi.js`、`src/components/visualizers/HanoiVisualizer.vue`、`src/views/HanoiView.vue`，以及 `src/utils/validators.js` 中 `validateHanoiInput`、`src/utils/generators.js` 中相关辅助函数，并在 `testCases.json` 中提交 TC-H-01（n=3，7 步）、TC-H-02（n=4，15 步）两条用例。具体工作包括：实现汉诺塔递归 `generateSteps`，每次合法盘子移动记录一步并附递归分解说明；用 Canvas 绘制三根柱及圆盘堆叠，展示移动前后状态；支持盘子数 n 手动输入（范围 3～8）与预设用例；终态展示总步数 2ⁿ−1 及完整移动序列。**不负责** 快排、Dijkstra 模块及公共组件、项目集成联调（由成员 1、2 负责）。验收标准：移动序列符合汉诺塔规则，逐步展示完整，n=3 为 7 步、n=4 为 15 步，2 条测试用例通过。 |

---

## 协作与依赖说明

| 协作项 | 说明 |
|--------|------|
| 接口约定 | 成员 1 最先输出 `generateSteps` 与 `ExecutionStep` 接口说明，成员 3～5 按此实现算法模块 |
| 用例汇总 | 成员 3～5 各自编写 2 条用例条目，成员 1 统一合并入 `testCases.json` 并校验格式 |
| 校验与生成 | `validators.js`、`generators.js` 可按函数拆分，各算法负责人维护本算法函数，合并冲突由成员 2 协调 |
| 联调节点 | 成员 2 在公共组件就绪后组织全站联调，成员 1 配合修复播放与会话相关问题 |

---

## 确认声明

本小组全体成员已充分沟通，确认以上分工详实具体，理解**提交后不可修改**，并同意按上表完成各自模块及验收标准。

| 序号 | 姓名 | 学号 | 签字 | 日期 |
|:----:|------|------|------|------|
| 1 | | | | |
| 2 | | | | |
| 3 | | | | |
| 4 | | | | |
| 5 | | | | |
